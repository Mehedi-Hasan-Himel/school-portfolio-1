/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // distDir: "dist",
  // output: "export",
};

module.exports = {
  images: {
    domains: ["i.ibb.co"],
  },
};

module.exports = nextConfig;
